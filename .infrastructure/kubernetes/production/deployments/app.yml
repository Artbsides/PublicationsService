kind: Deployment
apiVersion: apps/v1
metadata:
    name: publications-service-deployment
    namespace: production-apps
spec:
    replicas: 4
    selector:
        matchLabels:
            app: publications-service
    template:
        metadata:
            labels:
                app: publications-service
        spec:
            containers:
              - name: publications-service
                image: docker.io/rempeloliveira/public:publications-service.0.0.1
                imagePullPolicy: Always
                ports:
                  - containerPort: 8000
                envFrom:
                  - secretRef:
                        name: publications-service-secrets
                resources:
                    requests:
                        cpu: 250m
                        memory: 512Mi
                    limits:
                        cpu: 500m
                        memory: 1Gi
