kind: Service
apiVersion: v1
metadata:
    name: publications-service
    namespace: staging-apps
spec:
    selector:
        app: publications-service
    type: LoadBalancer
    ports:
      - name: http
        port: 8081
        protocol: TCP
        targetPort: 8000
